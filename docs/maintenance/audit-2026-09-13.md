# Full documentation and dead-code audit — 2026-09-13

Tree 3.1.1 → 3.1.2. Four read-only audit passes (two grok-4.6 xhigh, two Claude), every
finding verified against source before it was applied. Reports live outside the tree.

## What was wrong

| Area | Count | Examples |
|---|---|---|
| README / CLAUDE / AGENTS | 30 | routing described as skill-description driven (it reads `commands/*.md`); `/scc:write` and `/scc:refine` examples used positional files (`--input`, `--file`); Configuration JSON never loaded; standard-check `PASS` token does not exist; `.grok-plugin/` "not on main" |
| Architecture docs + diagrams | 58 | docs/ still stamped 3.1.0; a third phase vocabulary (Produce/Verify/Refine) in tables and mermaid; batch "five or more" (2–10); `proposals/` drawn under `cycles/`; hero SVG chip `interview` (folded into coach); roster SVG model tags overlapping job names |
| docs/skills guides | 75 | Pokemon filenames used as agent names in 10 guides; pdca `--domain` flag that does not exist; soul described as a tool-only command with wrong MCP fields and a wrong synthesis trigger; workflow `on_fail: skip` (schema says `continue`); missing `--file`, `--interactive`, `--context`, `--team-review`, `--max-cycles`, `--background` |
| Dead code | 8 fn + 5 const + 5 files | hook-side soul observation writers unused since commit 80072db (MCP `soul_record_observation` is the writer); `translations/` 37 MB of PDFs shipped to every plugin install |

## What changed

- Commits: `48b5fc7` (docs + diagrams), `84bc25d` (docs/skills), plus the dead-code and
  removal commit that follows this file.
- Removed: `translations/`, `docs/RELEASE-v*.md` (12), `scripts/usage-tracker.sh`,
  `config/config.example.json`, `docs/demo.tape`. Git history keeps them.
- Removed exports: `hooks/lib/soul-observer.mjs` detectSignals/appendObservation/
  readSoulProfile/readSoulReadiness/readLatestRetro; `hooks/lib/state-boundary.mjs`
  eventLogBoundaryIsSafe; `skills/unblock/engine/install.mjs` cacheDir;
  `skills/unblock/engine/cleaners/index.mjs` listCleaners.
- Kept on purpose: `templates/weekly-report-pipeline.json` (a `/scc:workflow` preset),
  `benchmarks/loop` (loop/evolve suites), `docs/proposals`, `docs/superpowers` (a contract
  test reads one spec), `docs/changelog-archive.md` (linked from CHANGELOG).
- discover guides keep `npx --yes skills find`: `tests/contracts/skill-contracts.test.mjs`
  pins it even though SKILL.md's Source Prioritization does not list it. Decide which is right.

## Verification

- `claude plugin validate .` ✔, contract tests 40/40, dead relative links 0.
- `npm test`: 625 pass / 5 fail — the same five failures as the clean tree before this
  audit (TypeScript consumer, CLAUDE_PLUGIN_DATA fixture, two coach interview cases,
  state-manager hook flow). CI has shown these since v3.1.0. Not touched here.
- Routing evals unchanged (no command description edited this round).

## Still open

- The five pre-existing test failures above.
- `docs/images/hero.svg` / `hero.ko.svg` are updated but embedded nowhere (README uses `thumbnail.png`).
- Command descriptions for `analyze`, `write`, `research`, `workflow` still lead with
  phase labels ("Do/Produce phase -- ..."). They pass the eval suite today; rewrite only
  with `claude plugin eval . --runs 3 --ablation none` as the gate.
- README chooser text is repeated three times (intro, "Choose an entry point", skills table);
  CLAUDE.md duplicates AGENTS.md. Left for a deliberate editorial pass.
