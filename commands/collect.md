---
description: "Plan/Gather phase -- collect knowledge into PARA-classified storage"
argument-hint: <input> [--search "query"]
---

Invoke the `/second-claude-code:collect` command to collect and organize knowledge through the `collect` skill.

## Arguments
- First argument: URL, text, or file path to collect
- `--search "query"` to search existing collections instead

## Your task
Perform the collection or search now using the plugin's loaded `collect` skill and the provided arguments.

- Return the final collection result or search result directly.
- Do not say that you are invoking or have invoked a skill.
