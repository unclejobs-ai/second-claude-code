---
description: "Capture knowledge -- URL, text, or file to PARA-classified storage"
argument-hint: <input> [--search "query"]
---

Invoke the `/second-claude-code:capture` command to capture and organize knowledge through the `capture` skill.

## Arguments
- First argument: URL, text, or file path to capture
- `--search "query"` to search existing captures instead

## Your task
Perform the capture or search now using the plugin's loaded `capture` skill and the provided arguments.

- Return the final capture result or search result directly.
- Do not say that you are invoking or have invoked a skill.
