{
  "name": "scc",
  "version": "1.5.2",
  "description": "PDCA-native Knowledge Work OS — skills, hooks, and MCP server for Claude Code",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  }
}
