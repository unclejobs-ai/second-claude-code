{
  "name": "scc",
  "version": "2.2.0",
  "description": "PDCA control loop for knowledge work — skills, hooks, and MCP server for Claude Code",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  }
}
